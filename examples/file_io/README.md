# File I/O Example

This example writes a small string to `out.txt` using `std::io::File` utilities.

## Build and run

From this directory:

```bash
glyph run
```

This creates `out.txt` in the project directory.
