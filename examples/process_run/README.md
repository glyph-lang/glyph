# Process Run Example

This example demonstrates launching an external process with `std/process::run`.

## Build and run

From this directory:

```bash
glyph run
```

The program returns the exit code of `true` (expected `0`).
