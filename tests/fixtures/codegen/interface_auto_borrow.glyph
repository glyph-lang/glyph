interface Drawable {
  fn draw(self: &Point) -> i32;
}

struct Point {
  x: i32
  y: i32

  impl Drawable {
    fn draw(self: &Point) -> i32 {
      ret self.x + self.y
    }
  }
}

fn main() -> i32 {
  let p = Point { x: 1, y: 2 }
  ret p.draw()
}
