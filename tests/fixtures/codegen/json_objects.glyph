from std/json import JsonValue
from std import println

fn main() -> i32 {
  // Test JSON object type
  let obj: Map<String, JsonValue> = Map::new()
  let obj_val = JsonValue::Object(obj)

  println($"JSON object type works")
  ret 0
}
