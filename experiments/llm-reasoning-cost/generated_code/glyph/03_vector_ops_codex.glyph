fn sum_positive(nums: Vec<i32>) -> i32 {
  let mut positives: Vec<i32> = Vec::new()

  for n in nums {
    if n > 0 {
      positives.push(n)
    }
  }

  let mut sum: i32 = 0
  for i in 0..positives.len() {
    sum = sum + positives[i]
  }

  sum
}