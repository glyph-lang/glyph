from std/vec import Vec

fn sum_positive(nums: &Vec<i32>) -> i32 {
  let mut positives: Vec<i32> = Vec::new()
  for n in nums {
    if n > 0 {
      positives.push(n)
    }
  }
  let mut sum = 0
  for p in positives {
    sum = sum + p
  }
  ret sum
}