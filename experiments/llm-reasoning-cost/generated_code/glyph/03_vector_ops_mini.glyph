```glyph
from std/vec import Vec

fn sum_positive(nums: Vec<i32>) -> i32 {
  let positives: Vec<i32> = Vec::new()
  for num in nums {
    if num > 0 {
      positives.push(num)
    }
  }
  let mut sum = 0
  for pos in positives {
    sum = sum + pos
  }
  ret sum
}
```