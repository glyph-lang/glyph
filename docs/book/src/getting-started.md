# Getting Started

Build the toolchain with Cargo, then use the `glyph` utility for day-to-day work:

```bash
cargo build --release
./target/release/glyph run
```

You can also compile a single file directly:

```bash
./target/release/glyph-cli build path/to/file.glyph
```
