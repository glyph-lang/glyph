# Standard Library

The standard library keeps a tight surface area: `std::io` for I/O, `std::vec` for vectors,
`std::map` for maps, `std::string` for strings, and `std::sys` for system access.

String utilities live on `String` and include `len`, `concat`, `slice`, `trim`, `split`,
`starts_with`, and `ends_with`.
